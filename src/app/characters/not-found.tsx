const NotFound = () => {
  return (
    <main className="not-found">
      <h1>Marvel character not found</h1>
      <p>Unfortunately, we could not find the requested page or character data.</p>
    </main>
  );
};

export default NotFound;
